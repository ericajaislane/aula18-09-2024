1 Apresente um algoritimo em C que leia 3 strings
A. Apresente o tamanho da primeira e da segunda.
b. Verifique se as duas primeiras são iguais. Se forem, copie o valor da terceira string para segunda. Se não, concatene o valor da priemria e da segunda na terceira. 
c.Imprima da tela os valores de cada string

#include <stdio.h>
#include <string.h>

int main() {
    char s1[100], s2[100], s3[100];

    printf("Digite a primeira string: ");
    fgets(s1, 99, stdin); 
    s1[strlen(s1)-1] = "\0";
    
    printf("Digite a segunda string: ");
    fgets(s2, 99, stdin); 
    s2[strlen(s2)-1] = "\0";
    
    printf("Digite a terceira string: ");
    scanf("%[^\n]", s3);
    printf("%s888", s3);
    
    return 0;
}
